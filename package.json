{
  "name": "clawtelemetry-repo",
  "private": true,
  "version": "0.0.1",
  "description": "Repo-level scripts for OpenClaw Dashboard and ClawTelemetry landing deployment",
  "scripts": {
    "deploy": "bash scripts/deploy-landing.sh",
    "deploy:landing": "bash scripts/deploy-landing.sh"
  }
}
