name: Test Install Script
on:
  push:
    paths: [install.sh, install.ps1, install.cmd]
  pull_request:
    paths: [install.sh, install.ps1, install.cmd]
  workflow_dispatch:

jobs:
  test-linux:
    runs-on: ubuntu-latest
    env:
      CLAWTELEMETRY_REF: ${{ github.sha }}
      CLAWTELEMETRY_REPO: ${{ github.repository }}
    steps:
      - uses: actions/checkout@v4
      - name: Run install script
        run: bash install.sh
      - name: Verify clawtelemetry binary
        run: clawtelemetry --version

  test-macos:
    runs-on: macos-latest
    env:
      CLAWTELEMETRY_REF: ${{ github.sha }}
      CLAWTELEMETRY_REPO: ${{ github.repository }}
    steps:
      - uses: actions/checkout@v4
      - name: Run install script
        run: bash install.sh
      - name: Add bin to PATH
        run: echo "$HOME/.local/bin" >> $GITHUB_PATH
      - name: Verify clawtelemetry binary
        run: clawtelemetry --version

  test-windows-ps:
    runs-on: windows-latest
    env:
      CLAWTELEMETRY_REF: ${{ github.sha }}
      CLAWTELEMETRY_REPO: ${{ github.repository }}
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run PowerShell install
        shell: pwsh
        run: .\install.ps1
      - name: Verify clawtelemetry binary
        shell: pwsh
        run: |
          $env:PATH = "$env:LOCALAPPDATA\clawtelemetry\Scripts;$env:PATH"
          clawtelemetry --version

  test-windows-cmd:
    runs-on: windows-latest
    env:
      CLAWTELEMETRY_REF: ${{ github.sha }}
      CLAWTELEMETRY_REPO: ${{ github.repository }}
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run CMD install
        shell: cmd
        run: install.cmd
      - name: Verify clawtelemetry binary
        shell: cmd
        run: |
          set PATH=%LOCALAPPDATA%\clawtelemetry\Scripts;%PATH%
          clawtelemetry --version

  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: ShellCheck
        run: shellcheck install.sh
